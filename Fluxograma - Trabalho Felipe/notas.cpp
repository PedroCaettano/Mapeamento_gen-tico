// apenas para salvar anotações

vector <int> tamanhoPopulacao = {10, 100, 1000};
double taxaCrossover, taxaMutacao;

std::vector<Individual> genetic_mapping(
    const std::vector<int>& population_sizes,
    double crossover_taxa,
    double mutation_taxa,
    int max_generations,

// Gerando valores para essas populações
for (int tamanho; tamanhoPopulacao) {
     vector<individuo> populacao;
     inicializarPopulacao(populacao, tamanho);

fir (int geracao = 0; geracao < )
}